using Microsoft.AspNetCore.Authentication.OpenIdConnect;

namespace AzureFileShareExplorer.Settings
{
    public class OpenIdConnectSettings : OpenIdConnectOptions
    {
        internal const string Name = "OpenIdConnect";
    }
}
